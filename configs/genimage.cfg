image sdcard.img {
    hdimage {
		partition-table-type = "none"
    }

    partition env {
        image = "env.img"
        offset = 0
        size = 32K
    }

    partition idblock {
        image = "rv1106_idblock_v1.15.102.img"
        offset = 0x8000
        size = 512K
    }

    partition uboot {
        image = "uboot.img"
        offset = 0x88000
        size = 256K
    }

    partition boot {
        image = "boot.vfat"
        offset = 4M
        size = 64M
    }

    partition rootfs {
        image = "rootfs.ext4"
        offset = 68M
    }
}
