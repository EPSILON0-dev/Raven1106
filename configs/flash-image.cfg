image flash.img {
    hdimage {
		partition-table-type = "none"
    }

    partition env {
        image = "env.img"
        offset = 0x0
        size = 0x8000
    }

    partition idblock {
        image = "idblock.img"
        offset = 0x8000
        size = 512K
    }
}
